nhung cai can sua :
- Khi submit form phai giu form lai cho nguoi dung doc lai thong tin cua khach hang

thong nhat :
- Chung csdl
- Chung giao dien
- Chung cach to chuc controllerJS

Hàm khóa màn hình chờ dữ liệu đc tải về rồi mới hiện ra
- Trang tra cứu : npmjs.com

- Từ khóa blockUI , nuget => blockUI angular

- Thêm đầy đủ thư viện js và css ở _LayoutMain và ở thẻ chúng ta muốn test chúng ta thêm thuộc tính block-ui="main" như cách ng-app , lưu ý : chổ ta khai báo block-ui sẽ là nơi chúng ta vạch 1 phạm vi có thể áp dụng được blockUI

- Ở trong common.js ta thêm tên thư viện 'blockUI' trong mảng các thư viện cùng với hàm cấu hình như sau :
 .config(function(blockUIConfig){
            'blockUI',
            blockUIConfig.delay = 200;
            blockUIConfig.autoBlock = true;
            blockUIConfig.blockBrowserNavigation = true;
        })

- Ở trong nơi js sử dụng chúng ta khai báo biến blockUI và chúng ta sẽ sử dụng blockUI.start() và blockUI.stop()


------------------
- Hàm tự động cập nhật file JS khi chúng ta reload mà chúng ta k cần crl + f5 , thầy sẽ gửi cái hàm này , hàm này chúng ta tạo ra

- Code :
public static class JavascriptExtension
    {
        public static MvcHtmlString IncludeVersionedJs(this HtmlHelper helper, string filename)
        {
            string version = GetVersion(helper, filename);
            return MvcHtmlString.Create("<script type='text/javascript' src='" + version + "'></script>");
        }
        private static string GetVersion(this HtmlHelper helper, string filename)
        {
            var context = helper.ViewContext.RequestContext.HttpContext;
            if (context.Cache[filename] == null)
            {
                var physicalPath = context.Server.MapPath(filename);
                var version = string.Format("{1}?v={0}", new System.IO.FileInfo(physicalPath).LastWriteTime.ToString("MMddHHmmss"), System.Web.VirtualPathUtility.ToAbsolute(filename));
                context.Cache.Add(filename, version, null,
                  DateTime.Now.AddMinutes(5), TimeSpan.Zero,
                  CacheItemPriority.Normal, null);
                return version;
            }
            else
            {
                return context.Cache[filename] as string;
            }
        }
    }


- Ở nơi sử dụng chúng ta sẽ link file script bằng : 
  @html.IncludeVersionedJS("Đường Link File Script");